# Generated by axiom (scripts/theme_lazygit.sh) from the "${THEME_NAME}" theme.
# Rewritten on every theme change: edit the template instead.
gui:
  theme:
    activeBorderColor: ["${BORDER_FOCUS}", bold]
    inactiveBorderColor: ["${BORDER}"]
    searchingActiveBorderColor: ["${WARNING}", bold]
    optionsTextColor: ["${ACCENT}"]
    selectedLineBgColor: ["${BACKGROUND_HIGHLIGHT}"]
    inactiveViewSelectedLineBgColor: ["${BACKGROUND_ALT}"]
    cherryPickedCommitFgColor: ["${BACKGROUND}"]
    cherryPickedCommitBgColor: ["${ACCENT_ALT}"]
    markedBaseCommitFgColor: ["${BACKGROUND}"]
    markedBaseCommitBgColor: ["${WARNING}"]
    unstagedChangesColor: ["${ERROR}"]
    defaultFgColor: ["${FOREGROUND}"]
