# Generated by axiom (scripts/theme_ghostty.sh) from the "${THEME_NAME}" theme.
# Rewritten on every theme change: edit the template instead.

background = ${BACKGROUND}
foreground = ${FOREGROUND}
cursor-color = ${ACCENT}
cursor-text = ${BACKGROUND}
selection-background = ${FOREGROUND}
selection-foreground = ${BACKGROUND}
palette = 0=${ANSI_0}
palette = 1=${ANSI_1}
palette = 2=${ANSI_2}
palette = 3=${ANSI_3}
palette = 4=${ANSI_4}
palette = 5=${ANSI_5}
palette = 6=${ANSI_6}
palette = 7=${ANSI_7}
palette = 8=${ANSI_8}
palette = 9=${ANSI_9}
palette = 10=${ANSI_10}
palette = 11=${ANSI_11}
palette = 12=${ANSI_12}
palette = 13=${ANSI_13}
palette = 14=${ANSI_14}
palette = 15=${ANSI_15}
