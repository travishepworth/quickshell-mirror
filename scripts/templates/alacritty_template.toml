# Generated by axiom (scripts/theme_alacritty.sh) from the "${THEME_NAME}" theme.
# Rewritten on every theme change: edit the template instead.

[colors.primary]
background = "${BACKGROUND}"
foreground = "${FOREGROUND}"

[colors.cursor]
text = "${BACKGROUND}"
cursor = "${ACCENT}"

[colors.selection]
text = "${BACKGROUND}"
background = "${FOREGROUND}"

[colors.normal]
black = "${ANSI_0}"
red = "${ANSI_1}"
green = "${ANSI_2}"
yellow = "${ANSI_3}"
blue = "${ANSI_4}"
magenta = "${ANSI_5}"
cyan = "${ANSI_6}"
white = "${ANSI_7}"

[colors.bright]
black = "${ANSI_8}"
red = "${ANSI_9}"
green = "${ANSI_10}"
yellow = "${ANSI_11}"
blue = "${ANSI_12}"
magenta = "${ANSI_13}"
cyan = "${ANSI_14}"
white = "${ANSI_15}"
